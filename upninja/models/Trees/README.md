# ✅ Описание пакета

Пакет Trees посвящен реализации алгоритмов на базе деревьев. Ниже - список модулей с подробным описанием.

## DecisionTreeClassifier.py (Decision Tree Classifier from Scratch)

Этот модуль реализует бинарное дерево решений для задачи классификации, построенное с нуля на основе критерия информационного прироста (Information Gain) и энтропии **или индекса Джини**. Реализация следует классическому алгоритму ID3/CART и полностью совместима с интерфейсом scikit-learn (`BaseEstimator`, `ClassifierMixin`).

---

### 🌲 Алгоритм построения дерева

Дерево строится рекурсивно, начиная с корневого узла, и продолжает разбиение до тех пор, пока не будут выполнены условия остановки:

- **Критерий остановки**:
  - Достигнута максимальная глубина дерева (`max_depth`)
  - Число объектов в узле меньше `min_samples`
  - Все объекты в узле принадлежат одному классу (энтропия = 0 или Джини = 0)

- **Выбор наилучшего разбиения**:
  - Для каждого признака рассматриваются возможные пороги разбиения.
    - По умолчанию — все средние между соседними уникальными значениями.
    - При указании параметра `bins` — используется гистограммное квантование для ограничения числа кандидатов на сплит (ускоряет обучение на больших данных).
  - Для каждого порога вычисляется **информационный прирост**:
    $\text{IG}(S, A) = I(S) - \sum_{v \in \{L, R\}} \frac{|S_v|}{|S|} I(S_v)$
    где $I(S)$ — мера неоднородности множества ($H(S)$ — энтропия или $G(S)$ — индекс Джини), а $S_L$, $S_R$ — левое и правое подмножества после разбиения.
  - Выбирается разбиение с **максимальным информационным приростом**.

- **Листовые узлы**:
  - Прогноз в листе — **мода** (наиболее часто встречающийся класс) среди объектов в этом узле.
  - Также сохраняется **оценка вероятности** класса 1 (для бинарной классификации), что позволяет использовать метод `predict_proba`.

---

### 🛠 Особенности реализации

- **Совместимость с scikit-learn**: класс наследует `BaseEstimator` и `ClassifierMixin`, поэтому поддерживает стандартные методы `.fit()`, `.predict()`, `.predict_proba()` и может использоваться в пайплайнах.
- **Гибкость критериев**: поддержка как `'entropy'`, так и `'gini'` через параметр `criterion`.
- **Оптимизация скорости**: опциональное ограничение числа порогов разбиения через параметр `bins` (гистограммное дерево).
- **Интерпретируемость**: автоматически вычисляется и сохраняется атрибут `feature_importances_`, основанный на взвешенном информационном приросте.
- **Безопасность API**: выбрасывает исключение при вызове `predict`/`predict_proba` до `fit`.
- **Требования к данным**: поддерживает только **бинарную классификацию**, числовые признаки и 1D-метки.
- **Разбиение по порогу**: все значения ≤ порога идут в левое поддерево, остальные — в правое.
- **Рекурсивное построение**: дерево строится "сверху вниз" с помощью рекурсивной функции `_build_tree`.