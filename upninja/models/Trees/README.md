# ✅ Описание пакета

Пакет Trees посвящен реализации алгоритмов на базе деревьев. Ниже - список модулей с подробным описанием.

## DecisionTreeClassifier.py (Decision Tree Classifier from Scratch)

Этот модуль реализует бинарное дерево решений для задачи классификации, построенное с нуля на основе критерия информационного прироста (Information Gain) и энтропии. Реализация следует классическому алгоритму ID3/CART и полностью совместима с интерфейсом scikit-learn (`BaseEstimator`, `ClassifierMixin`).

---

### 🌲 Алгоритм построения дерева

Дерево строится рекурсивно, начиная с корневого узла, и продолжает разбиение до тех пор, пока не будут выполнены условия остановки:

- **Критерий остановки**:
  - Достигнута максимальная глубина дерева (`max_depth`)
  - Число объектов в узле меньше `min_samples`
  - Все объекты в узле принадлежат одному классу (энтропия = 0)

- **Выбор наилучшего разбиения**:
  - Для каждого признака рассматриваются все уникальные значения как возможные пороги разбиения.
  - Для каждого порога вычисляется **информационный прирост**:
    $\text{IG}(S, A) = H(S) - \sum_{v \in \{L, R\}} \frac{|S_v|}{|S|} H(S_v)$
    где $H(S)$ — энтропия множества $S$, а $S_L$, $S_R$ — левое и правое подмножества после разбиения.
  - Выбирается разбиение с **максимальным информационным приростом**.

- **Листовые узлы**:
  - Прогноз в листе — **мода** (наиболее часто встречающийся класс) среди объектов в этом узле.

---

### 🛠 Особенности реализации

- **Совместимость с scikit-learn**: класс наследует `BaseEstimator` и `ClassifierMixin`, поэтому поддерживает стандартные методы `.fit()`, `.predict()` и может использоваться в пайплайнах.
- **Работа с числовыми признаками**: алгоритм поддерживает только числовые признаки и бинарные/многоклассовые метки.
- **Разбиение по порогу**: все значения ≤ порога идут в левое поддерево, остальные — в правое.
- **Рекурсивное построение**: дерево строится "сверху вниз" с помощью рекурсивной функции `build_tree`.